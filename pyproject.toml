[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "alpha-finder"
version = "0.1.0"
description = "Research toolkit for finding and backtesting equity alpha signals"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
